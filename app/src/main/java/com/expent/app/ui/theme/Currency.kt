package com.expent.app.ui.theme

import androidx.compose.runtime.staticCompositionLocalOf

/** The currently selected currency symbol, provided at the app root. */
val LocalCurrencySymbol = staticCompositionLocalOf { "₱" }
